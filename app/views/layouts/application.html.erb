<!DOCTYPE html>
<html>
<head>
  <title>Mari's Shopping Cart | <%= yield(:title) %></title>
  <%= stylesheet_link_tag    "application", :media => "all" %>
  <%= javascript_include_tag "application" %>
  <%= csrf_meta_tags %>
</head>
<body>
  <header class="navbar navbar-fixed-top">
    <div class="navbar-inner">
      <div class="container">
        <%= link_to "MSC", root_path, id: "logo" %>
        <nav>
          <ul class="nav pull-right">
            <li><%= link_to "Home", root_path %></li>
            <% if signed_in? %>
            <li><%= link_to "Profile", current_storekeeper %></li>
            <li><%= link_to "Sign out", storekeepersignout_path, method: "delete" %>
            <% else %>
            <li><%= link_to "Sign in", storekeepersignin_path %></li>
            <% end %>
          </ul>
        </nav>
      </div>
    </div>
  </header>
  <div class="container">
    <%= yield %>
  </div>
  </body>
</html>
