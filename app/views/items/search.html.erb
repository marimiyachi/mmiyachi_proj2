<% if @notice != nil %>
<h5><i><%=  @notice %></i></h5>
<br/>

<% else %>
<table>
  <% @items.each do |item| %>
    <tr>
      <td><%= link_to item.name, shop_url(item.store_id) %></td>
    </tr>
  <% end %>
</table>
<% end %>