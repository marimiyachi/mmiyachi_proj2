<%= form_for(@saved) do |f| %>
  <% if @saved.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@saved.errors.count, "error") %> prohibited this saved from being saved:</h2>

      <ul>
      <% @saved.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :storekeeper_id %><br />
    <%= f.number_field :storekeeper_id %>
  </div>
  <div class="field">
    <%= f.label :item_id %><br />
    <%= f.number_field :item_id %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
